//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1]";
    }

    function timeout0(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function ef(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function timeout0(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function timeout0(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function timeout0(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function timeout0(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function ef(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-10(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function ef(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function ef(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function ef(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function sfsf(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-11(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1]" && action=="timeout0"){
            currentStatus="[5, 1]";
            return true;
        }
        else
            return false;
    }

}