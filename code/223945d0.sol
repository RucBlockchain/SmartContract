//BCMETH means Blockchain Match Ethereum 
pragma solidity ^0.4.24;

contract BCMETH {
    String currentStatus;
    constructor () public {
        currentStatus="[1, 1, 1]";
    }

    function timeout0(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout2(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout0(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout0(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213213(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-09(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213213(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-09(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213213(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-09(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213123(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-11-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout0(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function (String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213213(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-09(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213213(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-09(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout2(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213213(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-09(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213213(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-09(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213123(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-11-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213213(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-10-09(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout2(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout1(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213123(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-11-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout2(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 213123(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-11-14(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function 123213(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function vio2018-11-23(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

    function timeout2(String actionStr) public returns(bool){
        if(currentStatus=="[1, 1, 1]" && action=="timeout0"){
            currentStatus="[5, 5, 5]";
            return true;
        }
        else
            return false;
    }

}